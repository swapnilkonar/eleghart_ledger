import 'package:flutter/material.dart';

class EleghartColors {
  // 🩶 Base Background (light metallic grey)
  static const Color bgLight = Color(0xFFE5E7EB);

  // 🩶 Metallic Neutral (to match shield)
  static const Color metalDark  = Color(0xFF2A2F38);
  static const Color metalLight = Color(0xFF3A3F4B);

  // 🔴 Accent (from deer logo)
  static const Color accentDark  = Color(0xFF8E1D1D);
  static const Color accentMid   = Color(0xFFB32626);
  static const Color accentLight = Color(0xFFC73535);

  // 🤍 Text colors
  static const Color textPrimary   = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint      = Color(0xFF9CA3AF);

  // 🩶 Input field background
  static const Color inputBg = Color(0xFFFFFFFF);

  // 🩶 Card background
  static const Color cardBg = Color(0xFFFFFFFF);

  // 🌫 Divider / subtle borders
  static const Color divider = Color(0xFFE5E7EB);
}
